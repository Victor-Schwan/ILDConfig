target-version = "py311"

line-length = 99

[lint]
extend-select = ["F"]
extend-ignore = ["F821"]  # undefined name from injected globals

[format]
# Make things format the same way as black
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
